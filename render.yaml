services:
  - type: web
    name: collab-workspace
    runtime: java
    plan: free
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/collab-workspace.jar
    healthCheckPath: /dashboard
    autoDeploy: true
